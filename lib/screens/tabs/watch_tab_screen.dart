import 'package:flutter/material.dart';

class WatchTabScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Watch Tab Screen'),
    );
  }
}
